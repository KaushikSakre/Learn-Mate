version: '3.9'

services:
  backend:
    image: kaushiksakre/learnmate-backend:latest
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1

  frontend:
    image: kaushiksakre/learnmate-frontend:latest
    ports:
      - "3000:80"
